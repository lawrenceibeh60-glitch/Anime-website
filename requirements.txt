# KYRO ANIME — Render.com Production Dependencies
# Render runs: pip install -r requirements.txt
# Then starts: gunicorn app:app

# === CORE ===
flask>=3.0.0
gunicorn>=21.0.0
requests>=2.31.0
beautifulsoup4>=4.12.0
user-agents>=2.2.0

# === SECURITY ===
flask-limiter>=3.5.0
flask-talisman>=1.1.0
flask-cors>=4.0.0
argon2-cffi>=23.1.0

# === REDIS (optional - for distributed rate limiting) ===
# redis>=5.0.0  # Uncomment if you add a Redis instance on Render
